import {Theme} from '@emotion/react';
import {SxProps} from '@mui/material';

export const myRewiewTextSx: SxProps<Theme> = {
    fontSize: '20px',
    margin: '40px 0 20px 0',
    textAlign: 'center',
    fontWeight: 500,
};
