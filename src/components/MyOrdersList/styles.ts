import {StylesDictionary} from '../../types/share';

export const useStyles = (): StylesDictionary => {
    return {
        list: {
            mt: '25px',
        },
        noFind: {
            textAlign: 'center',
            mt: '40px',
        },
    };
};
