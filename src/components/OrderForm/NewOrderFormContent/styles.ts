import {SxProps, Theme} from '@mui/material';

export const stackContainerStyles: SxProps<Theme> = {
    alignItems: 'center',
    mt: 3,
    gap: '10px',
    width: '100%',
};

export const inputStackStyles: SxProps<Theme> = {
    width: '100%',
};
